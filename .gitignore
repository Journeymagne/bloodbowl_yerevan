.env
.env.local
node_modules/
server-run*.log
*.log
.codex_tmp/
__pycache__/
scripts/__pycache__/
.github-upload/
_upload_git/
source/imports/

# Build output. README has always described dist/ as generated; it used to be
# committed anyway, which made every build show up as a 4.7 MB diff.
dist/

# Per-locale build output regenerates on every `npm run build` and is huge
# (~20k lines each); public/data.json (the pre-existing single-locale
# artifact) stays tracked per this repo's existing convention, but tracking
# these two as well would make every content-translation commit's diff
# unreviewable.
public/data.en.json
public/data.ru.json
