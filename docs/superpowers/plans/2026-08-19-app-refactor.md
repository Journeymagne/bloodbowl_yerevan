# План рефакторинга: экраны, организация информации, билдер, сохранение ростеров

> **Редакция 3 от 2026-08-28.** План сокращён с 122 шагов до пятнадцати. Что
> вычеркнуто и почему — раздел «Ревизия от 2026-08-28». Номера задач и шагов
> сохранены: на них ссылаются сообщения коммитов, `scripts/check-budgets.json`,
> design spec и разбор инцидента.

**Цель:** превратить одностраничный монолит (`src/app.js` — 7474 строки,
368 функций) в модульное приложение с одним редактором ростера, одним
каноническим форматом данных и надёжным сохранением.

**Архитектура:** vanilla JS + нативные ES-модули, без сборщика и без фреймворка.
`src/core/*` — инфраструктура, `src/domain/*` — чистая доменная логика
(переиспользуется сервером), `src/data/*` — хранение и синхронизация,
`src/screens/*` — экраны, `src/components/*` — компоненты. Сервер —
`server/routes/*`, `server/season/*`, `server/api/*`, `server/auth/*`,
`server/db/*`, `server/http/*`.

**Проектное решение:** `docs/superpowers/specs/2026-08-19-app-refactor-design.md`.
Раздел 10.2 спеки (формат RosterV2) **отменён** — см. «Правки к design spec».

---

## Состояние на 2026-08-28

| Что | Значение |
|---|---|
| `src/app.js` | 159 строк, только bootstrap (было 7474) |
| `server/server.mjs` | 147 строк (было 2040); сервер — 25 модулей |
| `src/` + `server/` | 13 411 строк в 97 файлах |
| Инструменты и тесты | 7 915 строк, которых не было |
| Тесты | 311; 307 проходят, 0 падают, 4 пропущены как POSIX-only |
| `npm run check` | зелёный: мёртвый код, ссылки через несделанный импорт, лимиты |
| `npm run build && npm run i18n:check` | зелёный: 292 страницы обеих локалей, 0 фолбэков |
| Багов найдено и починено по дороге | 25 коммитов `fix` |

**Список А закрыт**, кроме 7.8 — его владелец отложил сам. Закрыты целиком:
S (хотфикс безопасности), 1 (инструменты), 2 (гигиена), 3 (доменный слой),
4 (сервер), 6 (разбор `app.js`), 7 (общие части редактора), 8 (точечный
рендеринг), 11 (данные), 13.3 (ошибки на языке читателя), 14 (сезон),
15.4–15.6 (доступность). Почти закрыт 7b (общая разметка редакторов).

0.1 сделан наполовину: восстановление доказано на локальной базе, ночной дамп
с сервера остаётся за владельцем.

---

## Ревизия от 2026-08-28

Плану стало 122 шага, из них 76 не сделано. Ощущение бесконечности — не иллюзия,
но причина не в объёме работы, а в том, что **у плана была одна гранулярность**:
«шагом» назывались и «удалить мёртвый файл», и «построить постматчевый мастер».

Три вычеркивания и одно наблюдение.

**1. Задача 9 вынесена — это не рефакторинг, а фича.** Восемь шагов: таблица
снапшотов, трёхшаговый постматчевый мастер, статус Forfeit, новая механика
начисления SPP. Ничего этого в приложении нет. У плана есть собственное правило
«поведение UI не меняется», и задача 9 нарушала его целиком. Переехала в
`docs/superpowers/plans/2026-08-28-in-game-roster.md` и ждёт решения владельца,
делать ли её вообще. **Кроме шага 9.2** — он не фича, а закрытие потери данных,
и остаётся здесь.

**2. Задача 12 (15 шагов приёмки) вычеркнута.** Проект деплоится с каждым пушем
в `main`; финальная приёмка для него — церемония. Шаги 12.1–12.4 (`check`,
`test`, `build`, `i18n:check`) уже выполняются на каждом коммите. Осмысленный
остаток свёлся к пяти строкам в разделе «Приёмка».

**3. Задача 0 сжата с шести шагов до одного.** Ночные бэкапы работают с
2026-08-22, дампы снимаются сами. Живым остался один пункт: доказать, что дамп
разворачивается, — `DEPLOYMENT.md` до сих пор честно пишет, что это не
проверялось ни разу.

**4. Наблюдение, которое стоит записать: код вырос, а не уменьшился.**
`src/` + `server/`: 9 514 → 12 765 строк, файлов 10 → 114. План обещал «модульное
приложение» и получил его, но нигде не сказал, что модульность стоит объёма:
542 функции, каждая в своём файле, тянут импорты и заголовок. Плюс 2 046 строк
комментариев в продуктовом коде — 16%, около четырёх строк на функцию.

Это не ошибка и не повод чистить комментарии задним числом: шесть раз за
рефакторинг записанное «почему» помешало переломать обратно уже решённое. Но при
написании нового кода стоит помнить, что заголовок на десять строк перед
функцией на пять — перекос, и держаться ближе к трём.

---

## Что осталось: два списка

Оставшееся разделено по одному признаку — **заметит ли это тренер, открывший
сайт**. Первый список и есть остаток рефакторинга. Второй берётся, только если
попал под руку в соседней задаче; специально за ним ходить не нужно.

### А. Что заметит тренер

Порядок — по числу людей, которых это касается.

- [x] **11.1.** Коллекции хранят идентификаторы; `src/data/reference.mjs`
      разворачивает их при загрузке. Ни один потребитель `state.data.teams` не
      изменился — форма в памяти та же, и даже лучше: массивы держат **те же
      объекты**, что `pages`, а не копии. Заодно генерируемые файлы перестали
      печататься с отступами: 12% веса на индент в файле, который никто не
      открывает руками.
- [x] **11.2.** `text` — это `stripFormatting(body)`, то есть 0,19 МБ файла
      говорили то, что файл уже сказал. Сборка его не пишет, `reference.mjs`
      выводит на загрузке (один проход по 292 строкам). Стриппер переехал в
      `src/core/markdown.mjs`, чтобы сборка и браузер не разошлись; сверено на
      старом файле — все 292 страницы выводятся байт в байт.

      `html` остаётся: он тоже выводим, но только с индексом страниц для
      wiki-ссылок, и рендерить 292 страницы на загрузке — другая сделка.

- [x] **17.1.** Сборка пишет `.br` и `.gz` рядом с каждым файлом данных
      (качество 11, а не 5, которое мог себе позволить запрос), статика их
      отдаёт. Проверка меток времени — вся страховка: копия старше источника
      описывает файл, которого больше нет, поэтому игнорируется и запрос падает
      на сжатие на лету. Заодно раздача статики уехала в
      `server/http/static.mjs`; `server.mjs` — 164 строки.

      **Итог трёх шагов** — что качает посетитель в первый раз:

      | | было | стало |
      |---|---|---|
      | `data.en.json` | 1,80 МБ | **64 КБ** (brotli, готовый) |
      | `data.ru.json` | 2,32 МБ | **69 КБ** |

- [x] **9.2.** Каскад был трёхуровневый: `saved_teams` → `season_entries` →
      `season_pairings`. Тренер, прибиравший «Мои команды», уносил с собой
      каждый матч этой команды — включая счёт, записанный соперником, очки,
      которые тот заработал, и строки, из которых считается таблица. Миграция
      003 делает первое звено `RESTRICT`, API отказывает раньше и с
      объяснением.

      **Не только активный сезон**, намеренно: результаты законченного сезона
      — история лиги, и они перестают быть правдой, как только одна из команд
      в них исчезает. Каскад ниже (`season_entries` → `season_pairings`)
      оставлен: удаление участия — осознанное действие админа через отдельный
      эндпоинт, это другой вопрос.

- [x] **14.1.** Тренер, предложивший результат, больше не может подтвердить
      его сам. Двухшаговый процесс существует ровно ради того, чтобы счёт
      согласовали обе стороны; без этой проверки его согласовывал тот, кто
      дважды нажал. Админ по-прежнему может — для случая, когда соперник
      пропал. Экран матча показывает автору «результат отправлен», а не кнопки
      согласиться с собой.
- [x] **14.2.** Подтверждение — одна транзакция вместо двух запросов. Раньше
      сбой между ними оставлял пару со счётом и статусом, говорящим, что никто
      его не подтверждал. `updateSeasonPairing` принимает клиента, чтобы
      выполняться внутри чужой транзакции.

- [x] **10.3.** `GET /api/season` отвечает без сессии. `loadSeasonBundle`
      принимает `null` вместо пользователя и по этому решает, что опустить:
      `myEntry`, `currentFixture`, `myTeams`, админский блок.

      **И телеграм каждого тренера** — на этом стоит остановиться.
      `publicSeasonEntry` всегда его отдавал, потому что это способ
      договориться о матче: хорошая причина показывать его вошедшим и не
      причина публиковать в открытый интернет вместе с таблицей. Для
      неавторизованного он опускается; флаг проходит и через
      `computeSeasonStandings`, потому что таблица несёт те же записи.

      У клиента было зеркальное короткое замыкание: `loadSeason` возвращал
      пустой сезон, не спрашивая сервер. Это было верно, пока эндпоинт отвечал
      401, и сделало бы изменение невидимым.

- [x] **13.3.** Сервер возвращает коды, а не тексты: `{ error: { code, params } }`
      для всех `sendJson(4xx)` и `httpError`. Клиент рисует через
      `t("error.<code>")`. Механизм тот же, что для нарушений ростера (3.6) —
      новой инфраструктуры не заводить. Сейчас половина пользователей читает
      ошибки сервера по-английски.

      Сделано: `server/http/errors.mjs` — таблица `API_ERRORS` из 48 кодов,
      `httpError(status, code, params)` и `errorPayload()`. 115 английских
      строк в `server/` заменены кодами; четыре текста, которые собирались из
      подстановок, стали кодами с параметрами (`BODY_TOO_LARGE {limitKb}`,
      `ROUND_STILL_A_DRAFT {round}` и ещё два). Английский текст едет вместе с
      ответом как `message` — это запасной вариант при отсутствии ключа и это
      то, что видит человек с `curl`.

      На клиенте `ApiError` несёт `code` и `params`, а `errorText()` в
      `src/core/api.mjs` выбирает язык; 12 мест показа ошибок переведены на
      неё. `test/api-errors.test.mjs` падает, если у кода нет строки в одном
      из двух словарей или подстановки в переводе разошлись с оригиналом.

      Попутно: разделение 4.9 оставило в `server/server.mjs` 24 импорта, к
      которым больше никто не обращается. Удалены, 167 -> 148 строк.

- [x] **15.4.** Заменить `font-size` в пикселях (64 объявления) на `rem`, чтобы
      работала пользовательская настройка размера шрифта.

      Сделано делением на 16: корень остаётся на браузерном размере, так что
      1rem — это те же 16px по умолчанию, и ни один экран не поехал. Два
      `clamp()` заголовков переведены целиком, включая потолок: раньше он
      упирался в 46px независимо от настройки, теперь при корне 20px даёт
      51.2px. Проверено на живой странице.
- [x] **15.5.** Убрать `aria-live="polite"` с `#app-view`: программа чтения
      перечитывает весь экран при каждой правке. После задачи 8 перерисовка
      точечная, так что можно завести узкую область объявлений.

      Сделано: `src/components/live-region.mjs` — область за пределами экрана
      (`.sr-only`), в ней одно предложение за раз. Объявляются только те две
      вещи, которые зрячий читатель узнаёт взглядом: какой экран открылся
      (заголовок, после того как экран его нарисовал — многие ждут запрос) и
      чем кончилось автосохранение. «Не сохранено» и «сохраняю» приходят на
      каждое нажатие клавиши и молчат.

      Строка состояния переехала в
      `components/roster-editor/autosave-status.mjs`: у неё теперь два
      читателя. `test/autosave-status.test.mjs` держит правило «только
      результаты, и каждый один раз».

      Попутно нашлась причина, по которой браузер весь рефакторинг показывал
      старый код: модули импортируются без `?v=`, а сервер отдаёт их с
      `max-age=86400`. Исправлено отдельным коммитом — сборка проставляет
      версию на все импорты, а сама версия считается по всему графу модулей.
- [x] **15.6.** Добавить `:focus-visible` (сейчас 0 вхождений),
      `prefers-reduced-motion` и `prefers-color-scheme`.

      Четыре элемента управления гасили `outline` и предлагали взамен смену
      цвета рамки — на селекте, у которого рамка и так цветная. Убрано;
      кольцо рисуется по `:focus-visible`, то есть с клавиатуры, и не
      рисуется по щелчку мышью.

      `prefers-reduced-motion: reduce` гасит переходы и анимации: их всего
      четыре (боковое меню, затемнение, перетаскиваемая строка, тост), все
      декоративные.

      `prefers-color-scheme` работает только когда тему не выбирали: сайт
      открывался тёмным и тому, у кого система светлая, потому что тёмное ему
      читать тяжело. Теперь берётся `light-parchment`, а сохранённый выбор
      по-прежнему главнее; смена системной темы на ходу подхватывается, если
      выбора нет. Встроенный скрипт в `index.html` решает так же — иначе
      светлая тема мигала бы тёмной до загрузки стилей.

- [x] **4.7 (остаток).** Показать баннер выбора при конфликте. Серверная
      половина работает — `409` с текущей командой, — и стор доходит до
      состояния `conflict`, но баннер это разметка, а конфликт приходит между
      рендерами. Триггер нужен аккуратный: `subscribe()` переигрывает текущий
      статус, и каждый рендер подписывается заново.

      `wireConflictBanner()` в `components/roster-notices.mjs`: перерисовать,
      если статус стал `conflict`, а баннера на экране нет. Отсутствие
      баннера и есть защита от бесконечного цикла — после перерисовки он там
      есть. `test/roster-conflict-banner.test.mjs` держит оба случая.

      Проверено вживую, а не только тестом: тренер заведён через API, команда
      сохранена вторым клиентом, правка в открытой странице получила 409 —
      баннер появился между рендерами, фокус и каретка в поле остались на
      месте (6-й символ), текст правки не потерян.
- [x] **4.8.** Убрать дублирование логотипа: `logo_data` — единственное место
      хранения. `stripEmbeddedLogoData` остаётся защитой от старых клиентов, но
      `serializeRosterForStorage` должен получать ростер, в котором `logoData`
      уже нет по схеме.

      В `DRAFT_FIELDS` у `logoData` появилось `column: "logo_data"`, а
      `rosterForStorage()` выбрасывает такие поля из того, что уходит на
      сервер. Черновик логотип сохраняет — редактор его показывает. Оба
      запроса (создание и автосохранение) идут через эту функцию.

      Проверено: в собранном бандле `rosterForStorage()` действительно
      убирает поле, а в базе у команды с логотипом `roster ? 'logoData'`
      равно `f` при 118 байтах в колонке.

- [ ] **7.8.** *(Отложено владельцем до конца остальных правок.)* Интерфейс
      выбора прокачки: жмёшь «Улучшение», тратится SPP, тут же выбираешь, что
      получил. Модель готова (`advancements[].grants`, `canTakeAdvancement`).
      Вместе с ним чинится сообщение «Неизвестный тип прокачки: .» при нуле SPP.

- [~] **0.1.** Взять свежий ночной дамп с сервера, развернуть в локальный
      Postgres и прочитать целиком (`pg_restore -f /dev/null`, а не только
      `--list`). `DEPLOYMENT.md` до сих пор пишет, что восстановление не
      проверялось ни разу.

      Сделана половина, которая делается без доступа к серверу: дамп с
      локальной базы через `npm run backup:db`, разворот в отдельную базу,
      чтение всех блоков `pg_restore -f /dev/null`, сверка числа строк по
      каждой из восьми таблиц — совпало. Значит, порядок действий в
      `DEPLOYMENT.md` верный, а формат архива читается целиком.

      Осталась вторая половина, и она за владельцем: то же самое с настоящим
      ночным дампом с сервера. Локальная база маленькая и записана тем же
      кодом — она проверяет механику, но не данные. В `DEPLOYMENT.md` теперь
      написано именно это, вместо прежнего «не проверялось ни разу».

### Б. Что заметим только мы

Ни один из этих пунктов не меняет ничего для человека, открывшего сайт. Брать,
если оказался рядом; отдельного захода не стоит ни один.

- **6.10, 7b.7** — понизить оставшиеся исключения в `check-budgets.json` до
  общих 600 строк. Храповик и так не даёт им расти; цифра сама по себе никому
  не нужна.
- **7b.4, 7b.5** — `createRosterEditor()` и вынос `_saveEndpoint`/`_owner` в
  `permissions`. После 7b.1–7b.3 разметка уже общая, осталась разница в загрузке
  данных и проводке. Контракт `{ html, mount, destroy }` упирается в то же, во
  что упёрся шаг 8.3. **Ценность под вопросом** — решать, когда до этого дойдут
  руки, а не потому что записано.
- ~~**15.1**~~ — сделано: один файл на 4432 строки разрезан на восемь
  (`src/styles/`), `styles.css` остался списком из восьми `@import`. Резалось
  по позиции: каждый разрез проходит между правилами, которые и так стояли
  рядом, поэтому порядок каскада не изменился. Сборка склеивает импорты в один
  файл — собранный CSS вышел байт в байт таким же, как до разреза.
- ~~**15.2**~~ — сделано: базовые правила теперь описывают узкий экран, а
  широкий добирается в `@media (min-width: 601px / 761px / 901px)`, по
  возрастанию. Инвертированы все 73 правила трёх старых `max-width` блоков.

  Проверялось не на глаз: снят слепок вычисленных стилей (23 свойства) для
  всех элементов семи экранов на ширинах 375, 600, 760, 900, 901 и 1280 —
  до и после. Расхождений ноль. По дороге слепок поймал три настоящие
  поломки, которые иначе уехали бы в прод: `.builder-layout-main` и
  `.overview-grid` выигрывали тем, что стояли ниже в файле, а перенос их
  соседей в `@media` в конец это отнял; и правила верхней панели уехали бы
  на 901 вместо 761. Отдельно проверено выдвижное меню (375: открыть,
  закрыть; 1280: класс `nav-open` не должен делать ничего).

  Одно исключение осталось `max-width: 600px` и подписано в файле:
  `.table-scroll table` перебивает правила, которыми таблицы задают себе
  ширину сами (`.compact-roster-table` просит 980px), и «более широкого»
  правила, которое вернуло бы им управление, не существует.
- **15.3** — свести `.builder-*`/`.saved-*`/`.roster-*`. По самому плану имеет
  смысл, только если выпадет из 7b, а 7b не закрыт. Замерено: в
  `styles/roster.css` 27 групп правил с одинаковым телом, вместе это 208
  строк. Брать — после 7b, иначе то же место будет переписано дважды.
- ~~**15.7, 15.8**~~ — сделано. У вкладок сезона были роли, но не было
  остального: теперь вкладка называет панель (`aria-controls`), панель —
  вкладку (`role="tabpanel"`, `aria-labelledby`), в полосе один таб-стоп, а
  внутри работают стрелки, Home и End. Фокус после переключения переезжает на
  новую вкладку — перерисовка иначе роняет его в `body`.

  Вторая `<h1>` на главной стала `<h2>` (стиль поехал за уровнем).
  «К содержимому» — первая точка табуляции: до неё между верхом страницы и
  содержимым стояло 44 ссылки навигации. Это кнопка, а не ссылка: `href=
  "#app-view"` переписал бы хеш, а хеш — это роутер.
- ~~**10.6**~~ — сделано скромно: на карточке команды появились казна и
  метка «Играет в сезоне». Второе — то, чего не хватало больше всего: какая
  из сохранённых команд сейчас в игре, было видно только внутри редактора.
  Флаг сервер и так отдавал в списке (`in_active_season`), новых запросов не
  понадобилось; заодно он появился в `scripts/mock-api.mjs`, иначе браузерная
  проверка его не касается.
- ~~**10.1**~~ — сделано по решению владельца 2026-08-29. В меню было
  одиннадцать пунктов; осталось шесть: Обзор, Мои команды, Справочник, Мои
  игры, Сезон и (для админа) Администрирование.

  Inducements, Skills, Traits, Star Players и «Правила команд» ушли внутрь
  Справочника: на экране `#/pages` они идут первым списком «Разделы», каждый
  с числом записей, ниже прежние страницы под заголовком «Страницы». Пока
  читаешь любой из них, в меню подсвечен Справочник, а в шапке есть «Назад»
  на `#/pages`.

  Конструктор команды убран из меню совсем: в него входят кнопкой Create в
  «Моих командах» (и из регистрации в сезоне, где та же кнопка нужна по
  ходу дела). Пока он открыт, подсвечены «Мои команды», «Назад» ведёт туда
  же. Кнопка «Создать команду» с экрана «Правила команд» убрана — это была
  вторая дверь в тот же конструктор.
- **10.2, 10.5** — разведение названий «References»/«Team's Rules», решение
  про удалённые фильтры. Это вопросы к владельцу, а не задачи.
- **10.4** — перенести `overviewCards`/`overviewCardsRu` (631 строка правил лиги)
  в `content/Gata/Overview/*.md`. Соблюсти зеркальность из `CLAUDE.md`.
- ~~**11.4**~~ — сделано: `loadTranslations` не смотрел на статус ответа, и
  404 отдавал HTML-страницу ошибки в `JSON.parse` — приложение падало на
  «Unexpected token '<'», что читается как сломанная сборка, а не как
  отсутствующий файл. Теперь ошибка называет словарь и код.
- **11.3, 11.5** — разделить данные на каталог и тела страниц, кэширование
  новых файлов. 11.3 делать, только если 11.1–11.2 окажется мало.
- ~~**13.5**~~ — сделано: `scripts/check-i18n-strings.mjs` ищет английский,
  не прошедший через `t()`, в двух местах — текст между тегами и четыре
  говорящих атрибута (`title`, `placeholder`, `aria-label`, `alt`). Термины
  из глоссария разрешены. Проверка узкая намеренно: широкая давала тридцать
  ложных срабатываний и была бы выключена за неделю. Нашла четыре живых
  случая — все `aria-label`, то есть видимые только программе чтения, —
  они переведены. Добавлена в `npm run i18n:check`.
- ~~**13.1**~~ — сделано: пятнадцать описаний справочных страниц лежали
  английским `Map` по английскому заголовку. Заголовки этих страниц в обеих
  локалях одинаковые, так что русский тренер читал каталог по-английски.
  Теперь это ключи `preview.<id>` в обоих словарях — идентификатор страницы
  один и тот же в обоих трюмах. Страница без описания по-прежнему берёт
  начало собственного текста, то есть уже на языке читателя.
- ~~**13.4**~~ — сделано там, где это чинит проверяемость. Ключи вида
  `games.status.${status}` и `stats.${stat}` собираются на ходу, и найти их
  чтением исходников нельзя. Списки записаны:
  `GAME_RESULT_STATUSES` и `PLAYER_STATS` (последний лежал тремя копиями в
  трёх файлах), а `test/i18n-tables.test.mjs` проверяет, что у каждого
  значения есть строка в обоих словарях. Остальные два склеивания —
  `validation.<code>` и `error.<CODE>` — уже прикрыты своими тестами.
- **13.2** — остаток строк интерфейса.
- ~~**14.5**~~ — сделано, и это была не оптимизация, а ошибка: жадный проход
  объявлял повтор неизбежным там, где чистая пара существовала (четыре
  тренера, верхние двое уже играли — он сводил их снова). `server/season/
  pairing.mjs` ищет с возвратом, ближайший по таблице соперник первым, с
  ограничением по числу проб. Когда повтор действительно вынужден, сервер
  пишет в лог, сколько их и досчитал ли поиск. `generateSwissRound` 88 -> 83.
- ~~**14.3, 14.6**~~ — сделано. Очки за матч (3/1/0, плюс по одному за
  разрыв в три тачдауна, за сухой счёт и за четыре травмы) и порядок
  сортировки таблицы переехали в `src/domain/league-rules.mjs` —
  `matchPoints()` и `standingsOrder`. Раньше они жили внутри серверной
  функции, которая их применяет, то есть в единственном месте, куда никто не
  заглядывает: на вопрос «почему 4:0 стоит пять очков» сайт ответить не мог.
  `test/season-scoring.test.mjs` — 11 проверок: каждый бонус по отдельности,
  их сумма, 0:0 как ничья без сухого счёта, незапущенный раунд, пропуск как
  сыгранная игра, тай-брейки и устойчивый порядок при полном равенстве.
- ~~**14.4**~~ — отклонено владельцем 2026-08-28: Бухгольц не нужен.
  Равенство разводится тачдаунами, потом травмами, потом числом игр.
- ~~**16.2**~~ — сделано, и оказалось не косметикой. `meta.apothecary` вёз
  метку внутри значения ("Apothecary: Available" у всех 37 команд), а
  `canHireMedicalStaff` искал в этой строке английские слова. В русской
  локали строка переведена («Есть»), английских слов в ней нет — и русский
  тренер **не мог нанять аптекаря вообще**, ни одному из 37 составов.
  Сборка теперь выводит признаки доступа (`apothecaryAccess`) из английского
  трюма и кладёт одни и те же на обе локали; правило читает их, а не прозу.
  `cleanApothecary` удалён. Проверено в браузере: в русском конструкторе у
  Amazon появился Apothecary, у Nurgle — Plague Doctor.

  Попутно: версия ассетов не учитывала собранные данные, поэтому правка
  правил, не тронувшая код, могла сутки отдаваться из кэша. Учтена.

- ~~**16.1**~~ — сделано. `scripts/lib/validate-data.mjs` проверяет собранные
  данные обеих локалей и роняет сборку: нет ростера, нет цены у позиции, нет
  лимита, нет рероллов/лиги/аптекаря в мете, нет признаков доступа, повтор
  идентификатора, звёздный игрок без цены. Проверяется форма и наличие, а не
  правильность правил: неверная цена — вопрос содержания, отсутствующая —
  этот. Сообщения выводятся все сразу, до сорока.
- ~~**16.5**~~ — сделано: в списке дымовой проверки `DEPLOYMENT.md` стояли
  93 навыка и 53 свойства, а сборка выдаёт 86 и 38. Исправлено, и рядом
  написано, где смотреть первоисточник (`counts` в `data.en.json`), чтобы
  цифры не расходились снова.
- ~~**16.6**~~ — отменяется: `team-import:sql` в репозитории нет. Импорт
  ростеров целиком удалён 2026-08-21 по решению владельца (коммит 165ba7d),
  а файлы, на которые ссылался пункт, лежат в игнорируемом `.codex_tmp/`.
- ~~**17.2**~~ — сделано: `startSessionSweeper()` раз в шесть часов удаляет
  сессии с прошедшим сроком. Строка писалась на каждый вход и удалялась
  только при выходе, так что таблица только росла. Утечки в ней не было —
  просроченная строка не аутентифицирует, — но диск тот же, на котором лежит
  сезон. Таймер `unref()`, упавшая чистка не роняет сайт.
- ~~**17.4**~~ — сделано: деплой спрашивает `/api/health` до тридцати секунд
  и, если ответа нет, возвращает предыдущий коммит, пересобирает,
  перезапускает и роняет джобу. Откат только по коду: миграции
  только-вперёд, и в логе написано, до какого коммита откатывались.
- ~~**17.5**~~ — сделано: CSP блокирующий. Мешал встроенный скрипт темы —
  он уехал в `src/theme-boot.js` (обычный, не модуль: модуль отложенный и
  не успеет до первой отрисовки), а подмена `preload` на стиль через
  `onload` стала обычной ссылкой на таблицу стилей. Встроенного кода на
  странице не осталось вовсе, поэтому и `script-src`, и `style-src` теперь
  просто `'self'`. Проверено на собранном сайте под ровно этой политикой:
  нарушений нет, тема применяется, страницы рисуются.

  **Конфиг Caddy деплой не трогает** — `deploy/caddy/*.conf` нужно применить
  на сервере руками и перезагрузить Caddy.
- **5.9** — `_saveEndpoint`/`_owner` из объекта команды (едет с 7b.5).

---

## Глобальные ограничения

- **Стек не меняется:** vanilla JS, нативные ES-модули, без бандлеров и
  фреймворков. Единственная зависимость сервера — `pg`. Playwright опционален и
  в `dependencies` не попадает.
- **i18n-правило из `CLAUDE.md` действует без исключений.** Доменные функции
  возвращают **коды** нарушений, а не готовый текст.
- **Правило зеркальности контента из `CLAUDE.md` действует.** Перед коммитом
  контентных изменений: `npm run build && npm run i18n:check`.
- **Данные пользователей священны.** Необратимых операций над блобами в плане
  нет (шаги 3.3 и 4.4 отменены владельцем). Если такая появится — сначала 0.1.
- **Формат хранения не меняется.** `schema.mjs` описывает то, что уже лежит в
  базе; раздел 10.2 спеки отменён.
- **Храповик `scripts/check-budgets.json` только понижается.** Поднять можно
  ровно ради исправления бага или потери данных, и причина пишется в `_notes`
  того же файла.
- **Каждая задача заканчивается рабочим приложением.** Локальная база:
  `npm run postgres:up`, затем `npm run db:migrate`.

---

## Приёмка

Не отдельная задача, а то, что и так выполняется на каждом коммите, плюс три
вещи, проверяемые один раз в конце.

1. `npm run check` — 0 недостижимых функций, ни одной ссылки через несделанный
   импорт, лимиты соблюдены.
2. `npm test` — зелёный на Linux; на Windows зелёный либо с явным списком
   пропущенных и причиной.
3. `npm run build && npm run i18n:check` — равное число страниц обеих локалей,
   0 фолбэков, наборы ключей совпадают.
4. `npm start` + `npm run smoke`, ручной прогон `docs/smoke-test.md` на двух
   языках, `node scripts/browser-check.mjs` и `browser-check-roster.mjs`.
5. **Один раз в конце:** размер каталога данных уменьшился минимум вдвое; автор
   результата матча не может подтвердить его сам ни через интерфейс, ни через
   `curl`; таблица сезона открывается в приватном окне без входа; восстановление
   бэкапа проверено и дата записана в `DEPLOYMENT.md`.

---

## Что сделано, и чему это научило

Полная история — в git; здесь только то, что не выводится из кода.

**S — хотфикс безопасности** (закрыт 2026-08-22). Сервер отдавал по HTTP любой
файл каталога деплоя, включая `.env` и `.git/`. Белый список путей, смена
секретов, Postgres на `127.0.0.1`, `.env` в `/etc/bloodbowl-league/`. Разбор —
`docs/security-incident-2026-08-22.md`.

**1 — инструменты.** `npm run check` (мёртвый код, ссылки, лимиты),
`npm run smoke`, `npm test`, `scripts/mock-api.mjs`, два браузерных прогона.
Шаг 1.8 добавлен по ходу: проверка ссылки через несделанный импорт. Она нужна
потому, что `check-dead-code` ходит по достижимости и для него недостижимое имя
и несуществующее неотличимы; та же ошибка случилась шесть раз, и одну из них не
находил ни один запуск сервера.

**2 — гигиена.** `dist/` и `public/data*.json` из индекса, мёртвые ассеты
удалены, версия ассетов — хеш, а не ручной номер.

**3 — доменный слой.** `src/domain/roster/*` и `league-rules.mjs`. Поддержка
двух старых форм ростера удалена решением владельца; импорт/экспорт выпилен.
Форма черновика описана один раз, в `schema.mjs`, **без изменения того, что
лежит в базе** — `test/roster-schema.test.mjs` это замок: если его придётся
править, чтобы он позеленел, это миграция данных, а не рефакторинг.

**4 — сервер.** Миграции применяются командой, а не стартом: `init.sql`
выполнялся на каждом старте, и два его `UPDATE` работали безусловно, тихо
подтверждая результаты матчей. Сервер валидирует ростер теми же модулями, что и
браузер, — но отвергает только перебор и неизвестную расу, не недобор, потому
что редактор автосохраняется через состояния, где игроков временно шесть.
Оптимистичная блокировка по `revision`. Лимит тела запроса. Ограничение попыток
входа по имени логина. `handleApi` (672 строки) разобран на шесть модулей
маршрутов; `server.mjs` — 213 строк.

**5 — сохранение ростера.** `roster-store.mjs`: очередь на команду, точечное
слияние ответа сервера, зеркало в `localStorage`, `beforeunload`. Гонка «правка
сразу после сохранения» закрыта тестом. Попутно найден P0: `setTimeout` в
браузере требует `this === window`, и 131 тест в Node этого не ловил.

**6 — разбор `app.js`.** 7479 → 159 строк. Подписки состояния (6.3) и контракт
`render/mount/destroy` (6.4) так и не понадобились — см. шаги 8.2 и 8.3.

**7 и 7b — редакторы.** Общими стали пул найма, панель итогов, контрол
персонала, панель идентичности, каркас таблицы с колонками-данными. **Ячейки
слиты не были, и это решение:** у билдера 12 колонок и редактируемое только имя,
у лигового 19, и почти все — контролы. Один рендер строки потребовал бы флага
почти на каждую ячейку, то есть перенёс бы ветвление, а не убрал.
`alert`/`confirm` заменены на `toast`/`dialog`.

**8 — точечный рендеринг.** `patch()` в `core/dom.mjs`. Ключи оказались
обязательными: неключевые соседи сопоставляются по позиции, и появившийся баннер
несохранённых правок сдвигал всё за собой. Делегирование событий — предпосылка,
а не следствие: раз узлы переживают рендер, слушатель на выжившем узле
привяжется второй раз.

---

## Чего этот план сознательно не делает

Переход на фреймворк, TypeScript или сборщик; редизайн визуального стиля и тем;
изменение содержания правил лиги; онлайн-трекер матча по ходам; смена хостинга
или БД; отдельная мобильная версия; шифрование и офсайт-копия бэкапов.
Обоснование — раздел 12 design spec.

**Ростер в процессе игры** вынесен в
`docs/superpowers/plans/2026-08-28-in-game-roster.md` и ждёт решения владельца.

---

## Открытые вопросы

Одиннадцать вопросов разделов 13 и 21 design spec закрыты 2026-08-21. Осталось
два решения за владельцем:

- **Постматчевый мастер** — делать целиком, взять только запрет на удаление
  команды в сезоне (шаг 9.2, уже в списке А), или не делать.
- **Интерфейс выбора прокачки** (7.8) — отложен им же до конца остальных правок.

## Правки к design spec

Спека — документ обоснований, она не переписывается вслед за каждой задачей. Два
её места разошлись с принятыми решениями:

- **Раздел 10.2 (RosterV2) — отменён.** Форма данных не меняется; `schema.mjs`
  описывает то, что уже в базе.
- **Раздел 5.1 (12 пар функций-дублей)** — актуален, но из пяти оставшихся пар
  четыре слиты, а пятая (карточки для телефона) оставлена намеренно.
